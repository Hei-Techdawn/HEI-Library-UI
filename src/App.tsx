import {FC} from 'react';
import {BorrowingHistory} from "./Pages/Common/BorrowingHistory/BorrowingHistory";

export const App: FC = () => {
    return (
        <div>
            <BorrowingHistory/>
        </div>
    );
};
